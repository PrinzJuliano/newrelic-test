import { Routes } from '@angular/router';

export const routes: Routes = [
  {
    path: 'home',
    loadComponent: () => import('./home/home.page').then((m) => m.HomePage),
  },
  {
    path: '',
    redirectTo: 'home',
    pathMatch: 'full',
  },
  {
    path: 'navigate-page',
    loadComponent: () => import('./navigate-page/navigate-page.page').then( m => m.NavigatePagePage)
  },
];
